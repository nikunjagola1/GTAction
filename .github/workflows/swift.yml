name: Fastlane-5

on: [push]

jobs:
  ios-testflight:
    name: iOS Testflight
    runs-on: macOS-latest
    strategy:
        matrix:
          destination: ['platform=iOS Simulator,OS=12.2,name=iPhone X']

    steps:
      # - name: Force xcode 11
        # run: sudo xcode-select -switch /Applications/Xcode_11.1.app  

      - name: git Clone Cert 
        run:
          git clone https://nikunjagola1:1129db3522c4b2afc9fd32cb2d394a1e17c21a62@github.com/nikunjagola1/certsTest.git
      - name: List
        run: |
          ls
          cd cert
          ls

      - uses: actions/checkout@v1
          
      - name: Fastlane Build
        uses: maierj/fastlane-action@v0.10.0
        with:
          lane: 'beta'
